name: "Run IS qaqc package unit tests"

on:
  push:
    branches:
      - master
    paths:
      - pack/NEONprocIS.qaqc/**
  workflow_dispatch: {} # Allows trigger of workflow from web interface

env:
  # IS package name
  MODULE_BASE_NAME: NEONprocIS.base
  MODULE_BASE_PATH: ./pack/NEONprocIS.base
  MODULE_NAME: NEONprocIS.qaqc
  MODULE_PATH: ./pack/NEONprocIS.qaqc

jobs:
  run-unit-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5

      - name: Run IS ${{ env.MODULE_NAME }} unit tests
        uses: ./.github/actions/run-IS-unit-tests
