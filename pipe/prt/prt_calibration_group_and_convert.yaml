---
pipeline:
  name: prt_calibration_group_and_convert
transform:
  image: us-central1-docker.pkg.dev/neon-shared-service/neonscience/neon-is-cal-grp-conv:v3.0.0
  cmd:
  - sh
  - "-c"
  - |-
    /bin/bash <<'EOF'
    # Use bash-scrict mode. See http://redsymbol.net/articles/unofficial-bash-strict-mode/
    set -euo pipefail
    IFS=$'\n\t'
    
    # Refresh interim directories with each datum (otherwise they persist and cause probs)
    rm -rf $OUT_PATH
    rm -rf /tmp/pfs/kafka_merged
    mkdir -p $OUT_PATH # R modules must have pfs in the repo structure
    mkdir -p /tmp/pfs/kafka_merged # Filter joiner relies on the same path positions among inputs (i.e. repo name in 2nd position)
    
    # Join files from the archive and from Kafka
    python3 -m filter_joiner.filter_joiner_main
    
    # Combine data from Kafka and the archive
    Rscript ./flow.kfka.comb.R \
          DirIn=$OUT_PATH \
          DirOut=/tmp/pfs/kafka_merged \
          DirErr=/pfs/out/errored_datums \
          FileSchmL0=$FILE_SCHEMA_L0 \
          DirSubCopy=calibration

    # Run calibration conversion module
    Rscript ./flow.cal.conv.R \
      DirIn=/tmp/pfs/kafka_merged \
      DirOut=/pfs/out \
      DirErr=/pfs/out/errored_datums\
      FileSchmData=$FILE_SCHEMA_DATA \
      FileSchmQf=$FILE_SCHEMA_FLAGS \
      FileUcrtFdas=$FILE_UNCERTAINTY_FDAS \
      ConvFuncTerm1=def.cal.conv.poly:resistance \
      "TermQf=resistance" \
      "UcrtFuncTerm1=def.ucrt.meas.cnst:resistance" \
      "UcrtFuncTerm2=def.ucrt.fdas.rstc.poly:resistance"

    EOF
  env:
    # Environment variables for filter-joiner
    CONFIG: |
      ---
      # Configuration for filter-joiner module that will bring together the data and calibrations
      # In Pachyderm root will be index 0, 'pfs' index 1, and the repo name index 2.
      # Metadata indices will typically begin at index 3.
      input_paths:
        - path:
            name: KAFKA_UNMERGED_DATA
            # Filter for data directory
            glob_pattern: /pfs/KAFKA_UNMERGED_DATA/prt/*/*/*/*/**
            # Join on named location (already joined below by day)
            join_indices: [7]
            outer_join: true
        - path:
            name: DATA_PATH_ARCHIVE
            # Filter for data directory
            glob_pattern: /pfs/DATA_PATH_ARCHIVE/prt/*/*/*/*/**
            # Join on named location (already joined below by day)
            join_indices: [7]
            outer_join: true
        - path:
            name: CALIBRATION_PATH
            # Filter for data directory
            glob_pattern: /pfs/CALIBRATION_PATH/prt/*/*/*/*/**
            # Join on named location (already joined below by day)
            join_indices: [7]
            outer_join: true
    OUT_PATH: /tmp/pfs/filter_joined # Note that R modules use "pfs" in the path structure to determine datums
    LOG_LEVEL: INFO
    RELATIVE_PATH_INDEX: "3" # Must be consistent across inputs 
    LINK_TYPE: COPY # options are COPY or SYMLINK. MUST BE SIMLINK IF USING COMBINED MODULE.
    # Environment variables for calibration module
    PARALLELIZATION_INTERNAL: '5' # Option for calibration conversion module
input:
  cross:
  - pfs:
      name: FILE_SCHEMA_L0
      repo: prt_avro_schemas
      glob: /prt/prt.avsc
  - pfs:
      name: FILE_SCHEMA_DATA
      repo: prt_avro_schemas
      glob: /prt/prt_calibrated.avsc
  - pfs:
      name: FILE_SCHEMA_FLAGS
      repo: prt_avro_schemas
      glob: /prt/flags_calibration_prt.avsc
  - pfs:
      name: FILE_UNCERTAINTY_FDAS
      repo: prt_uncertainty_fdas
      glob: /fdas_calibration_uncertainty_general.json
  # Outer join all days so that varying sensors between kafka and trino loaders will all get joined with calibrations. Filter-joiner will narrow down.
  - join:
    - pfs:
        name: CALIBRATION_PATH
        repo: prt_calibration_assignment
        glob: /prt/(*)/(*)/(*)
        joinOn: $1/$2/$3
        outer_join: true
        empty_files: false # Make sure to use false if LINK_TYPE=COPY. Can also be set to false for LINK_TYPE=SYMLINK.
    - pfs:
        name: DATA_PATH_ARCHIVE
        repo: prt_data_source_gcs
        glob: /prt/(*)/(*)/(*)
        joinOn: $1/$2/$3
        outer_join: true
        empty_files: false # Make sure to use false if LINK_TYPE=COPY. Can also be set to false for LINK_TYPE=SYMLINK.
    - pfs:
        name: KAFKA_UNMERGED_DATA
        repo: prt_data_source_kafka
        glob: /prt/(*)/(*)/(*)
        joinOn: $1/$2/$3
        outer_join: true
        empty_files: false # Make sure to use false if LINK_TYPE=COPY. Can also be set to false for LINK_TYPE=SYMLINK.
parallelism_spec:
  constant: 10
autoscaling: true
resource_requests:
  memory: 2.5G
  cpu: 5.5
resource_limits:
  memory: 4G
  cpu: 7
sidecar_resource_requests:
  memory: 3.5G
  cpu: 0.6
datum_set_spec:
  number: 1
scheduling_spec:
  node_selector:
    cloud.google.com/compute-class: pach-pipeline-class
