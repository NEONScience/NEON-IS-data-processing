---
pipeline:
  name: subsurfMoorTempCond_group_assignment
transform:
  cmd: ["/bin/bash"]
  stdin:
  - "#!/bin/bash"
  - export ERR_PATH="/pfs/out/errored_datums$FILE_YEAR"
  - Rscript
      ./flow.loc.grp.asgn.R
      DirIn=$DIR_IN
      DirOut=/pfs/out
      DirErr=$ERR_PATH
      FileYear=$FILE_YEAR
      TypeFile=group
  image: us-central1-docker.pkg.dev/neon-shared-service/neonscience/neon-is-loc-grp-asgn:v1.2.1
  env:
    LOG_LEVEL: INFO
input:
  cross:
  - pfs:
      name: DIR_IN
      repo: subsurfMoorTempCond_group_loader
      glob: /subsurf-moor-temp-cond/*
  - pfs:
      name: FILE_YEAR
      repo: lt400_hu24_cron_daily_and_date_control
      glob: /data_year*.txt
parallelism_spec:
  constant: 3
autoscaling: true
resource_requests:
  memory: 250M
  cpu: 1
resource_limits:
  memory: 400M
  cpu: 2
sidecar_resource_requests:
  memory: 1G
  cpu: 0.5
datum_set_spec:
  number: 5
scheduling_spec:
  node_selector:
    cloud.google.com/compute-class: pach-pipeline-class
