---
pipeline:
  name: cron_daily
transform:
  cmd: ["/bin/bash"]
input:
  cron:
    name: tick
    spec: "0 7 * * *" # Run at 00:00 MST (07:00 GMT)
    overwrite: true
autoscaling: true
scheduling_spec:
  node_selector:
    cloud.google.com/compute-class: pach-pipeline-class

