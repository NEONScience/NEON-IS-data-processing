---
pipeline:
  name: cron_yearly
transform:
  cmd: ["/bin/bash"]
input:
  cron:
    name: tick
    spec: "@yearly"
    start: "2021-01-01T00:00:00.000Z"
    overwrite: true
autoscaling: true
scheduling_spec:
  node_selector:
    cloud.google.com/compute-class: pach-pipeline-class
