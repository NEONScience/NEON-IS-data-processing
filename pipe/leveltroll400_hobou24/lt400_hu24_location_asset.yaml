---
pipeline:
  name: lt400_hu24_location_asset
transform:
  image: us-central1-docker.pkg.dev/neon-shared-service/neonscience/neon-is-loc-asset-loader:v1.1.0
  cmd:
  - /bin/bash
  stdin:
  - '#!/bin/bash'
  - export SOURCE_TYPE=$SOURCE_TYPE_1
  - python3 -m location_asset_loader.location_asset_loader_main
  - export SOURCE_TYPE=$SOURCE_TYPE_2
  - python3 -m location_asset_loader.location_asset_loader_main
  env:
    OUT_PATH: /pfs/out    
    # ERR_PATH can be changed, it is user specified
    ERR_PATH: /pfs/out/errored_datums
    LOG_LEVEL: INFO
    SOURCE_TYPE_1: leveltroll400
    SOURCE_TYPE_2: hobou24
  secrets:
  - name: pdr-secret
    mount_path: /var/db_secret
input:
  pfs:
    repo: lt400_hu24_cron_daily_and_date_control_tick
    glob: /*
    empty_files: true
autoscaling: true
resource_requests:
  memory: 100M
  cpu: 0.15
resource_limits:
  memory: 300M
  cpu: 0.5
sidecar_resource_requests:
  memory: 500M
  cpu: 0.2
scheduling_spec:
  node_selector:
    cloud.google.com/compute-class: pach-pipeline-class
